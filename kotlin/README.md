# kotlin

Kotlin CLI application.

## Requirements

- JDK 17+
- Gradle (the Gradle wrapper `./gradlew` is included, no separate install needed)

## Structure

```
kotlin/
└── README.md   ← you are here
```

## Getting started

```bash
# Build
./gradlew build

# Run
./gradlew run
```
