# CLI


## Conventions

- don't use short options
- long options
- by default, show `--help`
- `--help` should have the standardized output from #todo
- `kotlincli <CMD> `
- `pythoncli <CMD>`
- `make links` should create links and wrapper scripts inside the `$REPO/cli/bin` folder