## Makefile

- `make` run all the above
- `make doctor` checks the requirements
- `make sync` checks that everything is synced with GitHub, or propose git commands to make that happen
- `make links` creates symlinks inside `bin`
- `make kotlin`
- `make typescript`
- `make python`


